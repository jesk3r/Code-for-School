import pygameimport sysimport timeimport randompygame.init()screen_width = 800screen_height = 640black = (0,0,0)white = (255,255,255)red = (176,0,0)green = (0,200,0)blue = (36, 115, 148)dark_red = (149,0,0)dark_blue = (26, 101, 134)FPS = 0.5screen = pygame.display.set_mode((800,640))gameDisplay = pygame.display.set_mode((screen_width, screen_height))pygame.display.set_caption('CivX')clock = pygame.time.Clock()def text_objects(text, font):    textSurface = font.render(text, True, white)    return textSurface, textSurface.get_rect()def title_screen(text):    largeText = pygame.font.Font('Megrim.ttf', 115)    TextSurf, textRect = text_objects(text, largeText)    textRect.center = ((80),(200))    gameDisplay.blit(TextSurf, textRect)    pygame.display.update()    time.sleep(2)    game_intro()def button(msg, x, y, w, h, ic, ac,action=None):    mouse = pygame.mouse.get_pos()    click = pygame.mouse.get_pressed()    print(click)    #print(mouse)    if x+w > mouse[0] > x and y +h > mouse [1] > 246:            pygame.draw.rect(gameDisplay, ac, (x,y,w,h))            if click [0]==1 and action != None:                #print("Left Key Pressed")                if action == "Play":                    game_loop()                elif action == "Quit":                    pygame.quit                    quit()    else:        pygame.draw.rect(gameDisplay, ic, (x,y,w,h))    smallText = pygame.font.Font('Megrim.ttf', 20)    TextSurf, textRect = text_objects(msg, smallText)    textRect.center = ((x+(w/2),(y+(h/2))))    gameDisplay.blit(TextSurf, textRect)def game_intro():    intro = True    while intro:        for event in pygame.event.get():            if event.type == pygame.QUIT:                pygame.quit()                quit()        largeText = pygame.font.Font('Megrim.ttf', 65)        TextSurf, textRect = text_objects("CivX", largeText)        textRect.center = ((80),(200))        gameDisplay.blit(TextSurf, textRect)        #def button(msg, x, y, w, h, ic, ac):        button ("Start", 22, 246, 130, 40, blue, dark_blue, "Play")        button ("Quit", 27, 306, 116, 34, red, dark_red, "Quit")        pygame.display.update()        clock.tick(100)        class Background(pygame.sprite.Sprite):    def __init__(self, image_file, location):        pygame.sprite.Sprite.__init__(self)  #call Sprite initializer        self.image = pygame.image.load(image_file)        self.rect = self.image.get_rect()        self.rect.left, self.rect.top = locationBackGround = Background('Main Menu-1 (dragged).png', [0,0])screen.blit(BackGround.image, BackGround.rect)pygame.display.update()screen.fill((0,0,0))clock.tick(12)image2 = pygame.image.load('Main Menu-2 (dragged).png')screen.blit(image2, [0,0])pygame.display.update()clock.tick(12)game_intro()pygame.quit()quit()